# Google Gemini AI API Key - use to convert the transcript, received from YouTube-Transcript.io, into an annotation that should be displayed on the app page

GEMINI_API_KEY=AIzaSyD3ZVaL9WlRNT6hznb0dsdJnLEQn_5kv6s

#exa
EXA_API_KEY=c2e1fb8d-6520-4cfe-92d1-659f422661d7

# STREAMING_ENABLED управляет тем, разрешает ли бэкенд использование стримингового эндпоинта
# (выключено): /chat_stream возвращает ошибку 403, и расширение переключается на использование /chat (один JSON-ответ).
# 1 (включено): /chat_stream возвращает текстовый поток (text/plain) с инкрементными токенами. Для этого также требуется GEMINI_API_KEY; без этого ключа /chat_stream возвращает 403.
# Устанавливайте 0 во время отладки или при нестабильной сети, чтобы избежать зависаний и сохранить простоту поведения.
# Устанавливайте 1 для рабочей среды или когда вам нужны постепенные обновления по токенам и у вас есть действительный GEMINI_API_KEY (и, опционально, EXA_API_KEY).

STREAMING_ENABLED=0

# парамтеры визуализации графа
LANGGRAPH_VIZ=save
LANGGRAPH_VIZ_FORMAT=svg
LANGGRAPH_VIZ_PATH=/home/alexandre/Рабочий стол/chrome-lg/backend_lg/app/_graph.svg

# размер чанка (символы), по умолчанию 3500
LG_CHUNK_SIZE=1200

# перекрытие чанков (символы), по умолчанию 150
LG_CHUNK_OVERLAP=180

# минимальная общая длина текста для включения разбиения. Если 0, используется 2×LG_CHUNK_SIZE
LG_CHUNK_MIN_TOTAL=2400

# максимум чанков для извлечения заметок, по умолчанию 6
LG_CHUNK_NOTES_MAX_CHUNKS=6

# максимум заметок, по умолчанию 12
LG_NOTES_MAX=12

# “мало контекста” для эвристики, по умолчанию 1000
LG_SEARCH_MIN_CONTEXT_CHARS=1000

# сколько символов TEXT включать в промпт, по умолчанию 4000
LG_PROMPT_TEXT_CHARS=3000

# бюджет времени EXA-узла, по умолчанию 2.5
LG_EXA_TIME_BUDGET_S=2.5

# включить мягкие эвристики поиска, по умолчанию выключено (1/true/yes/debug — включить)
LG_SEARCH_HEURISTICS=0
